//  here we see the how to  decalre the dynamic memory in the programme  

// preprocessor 
#include<iostream> 
using namespace std;


// main methods 
int main(){
    // dynamic memory allocation of the data 
    //example is
    // linked list 
    // 4 type of the linnked list 
    // singuler 
    // circular singuler 
    // doubly ll 
    // circular doubly linked list 

    // all the linked is store in the heap 

    // dynamic memory 
    // stack and queue 
    


    // syntax of the dynamic memort allocation 

    // pointer is used for  the declare the dynamic memory 
    // memory  allocation is done at the time of the runing the programme 

    // new keyword is used for  the dynamic memory allocation 
    // when we used the new keywords it create the memory in heap 
    // by default 


    //  int *m = NULL; // intially  we hvae a null pointer 
    // m= new int ;// int is data type // now size of the m = 4 byte 
    // 1 byte = 8 bits 
    // 4*8 = 32 bits 


    // we can do in to the single line of code 
    int *m = new int(10) ; 

    printf("%d",*m);
    // before initlaizing the  memory first 
    //we have to check the space is  avilables or not 
    // in heap 

    // 


}
