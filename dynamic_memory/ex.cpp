// preprocessor 
#include<iostream>
using namespace std; 
// main methods 
int main(){
    // local variables 
    int a = 34; 
    // here we see the used of the pointer 
    // pointer is used for the dynamic memory allocation 
    int  *ptr ; 
    ptr = &a;

    // we can used the printf()
    // formate  for the  while using the formate specifiers in the cpp 

    
    printf("%d",ptr);
    // cout<<dptr<<endl;
}