#include<stdio.h>
#include<conio.h>
int main(){
    // pointer can be null 
    // if we have don't have any value then we can store the null 
    // so this is know as null pointer

    // when pointer pointing  to the null value 
    // let see the example 
    int *ptr = NULL; // value is 000000000  
    printf("%p",ptr);
    return 0;
}